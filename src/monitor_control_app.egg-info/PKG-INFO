Metadata-Version: 2.4
Name: monitor-control-app
Version: 0.1.0
Summary: Desktop tool for switching monitor input sources through DDC/CI.
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: monitorcontrol>=3.1.0
Requires-Dist: PySide6>=6.6.0
Provides-Extra: dev
Requires-Dist: pytest>=8.0.0; extra == "dev"
Requires-Dist: pyinstaller>=6.0.0; extra == "dev"

# Monitor Control

桌面端显示器输入源切换工具，基于 Python、PySide6 和 `monitorcontrol`。

## 开发运行

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -e .[dev]
python -m monitor_control_app
```

## 功能

- 自动扫描支持 DDC/CI 的显示器。
- 读取并展示当前输入源。
- 支持 DP、USB-C、HDMI1、HDMI2、DVI 快捷切换。
- 支持 Windows/Mac 双端互切主按钮。
- 支持自定义输入源代码和设备映射。
- 支持仅当前显示器或全部显示器批量切换。
- 支持最小化到系统托盘，并可通过托盘右键菜单切换输入源或退出。

## 注意

显示器必须支持并启用 DDC/CI。不同品牌显示器的输入源 VCP 值可能不同，可在设置中修改映射。
